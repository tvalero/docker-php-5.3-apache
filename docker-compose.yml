version: '2'
services:
  
  php-apache:
    build:
      context: ./
      dockerfile: Dockerfile
    image: quay.io/mivegec/docker-php-5.3-apache:2020-09-21
    restart: always


networks:
  default:
     driver: bridge 
