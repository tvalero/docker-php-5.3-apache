version: '2'
services:
  
  php-apache:
    build:
      context: ./
      dockerfile: Dockerfile
    image: quay.io/mivegec/docker-php-5.3-apache:2018-11-20
    restart: always


networks:
  default:
     driver: bridge 
